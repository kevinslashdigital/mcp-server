[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    unit: marks tests as unit tests
    integration: marks tests as integration tests
    real_integration: marks tests as real integration tests that call actual Jira API
    slow: marks tests as slow running
